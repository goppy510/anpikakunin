"use client";

import { useEffect } from "react";

export default function OauthPage() {
  useEffect(() => {
    // 相当する ngOnInit の処理（まだ空なので何もしない）
    // 将来的に oauth2 処理などが入る可能性あり
  }, []);

  return (
    <div>
      <p>oauth works!</p>
    </div>
  );
}
